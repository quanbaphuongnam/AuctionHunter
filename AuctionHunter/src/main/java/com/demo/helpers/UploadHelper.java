package com.demo.helpers;

import java.nio.file.Files;
import java.nio.file.Path;
import java.nio.file.Paths;

import javax.servlet.ServletContext;

import org.springframework.web.multipart.MultipartFile;

public class UploadHelper {
	/*
	 * public static String upload(ServletContext servletContext,MultipartFile file)
	 * { try { String fileName= generateFileName(file.getOriginalFilename()); byte[]
	 * bytes = file.getBytes(); Path path =
	 * Paths.get(servletContext.getRealPath("/assets/uploads/" +
	 * file.getOriginalFilename())); Files.write(path, bytes); return
	 * file.getOriginalFilename(); } catch (Exception e) { return null; } } public
	 * static String generateFileName(String fileName) { try { String fileName=
	 * generateFileName(file.getOriginalFilename());
	 * 
	 * } catch (Exception e) { return null; } }
	 */
}
